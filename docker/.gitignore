# Environment files (contain secrets)
.env
!.env.example

# Docker compose override (generated by setup.sh)
docker-compose.override.yml

# Secrets directory (CRITICAL - never commit)
secrets/
!secrets/.gitkeep

# WireGuard configs (contain private keys)
wireguard/
!wireguard/.gitkeep

# Custom configs
config/
!config/.gitkeep

# Host binaries (mounted from host)
bitcoin-cli

# Build artifacts
*.tar
*.gz

# Backup files
*.backup.*
backups/

# Log files
*.log
logs/
