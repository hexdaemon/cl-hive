# This directory stores Docker secrets for production deployments.
# Files in this directory should NEVER be committed to version control.
#
# Expected files:
#   - bitcoin_rpc_password  (Bitcoin Core RPC password)
#   - wg_private_key        (WireGuard private key, if VPN enabled)
#   - hive_rune             (CLN rune for API access, if needed)
#   - backup_gpg_key        (GPG private key for encrypted backups)
#
# Created by: ./setup.sh
